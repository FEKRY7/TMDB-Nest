export enum UserType {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum Genre {
  ACTION = 'Action',
  COMEDY = 'Comedy',
  HORROR = 'Horror',
  DRAMA = 'Drama',
}

export enum ListType {
  FAVORITE = 'FAVORITE',
  WATCHLIST = 'WATCHLIST',
}